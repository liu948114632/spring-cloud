server:
  port: 8000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka
    fetch-registry: false    #自己不注册
    register-with-eureka: false
spring:
  application:
    name: eureka